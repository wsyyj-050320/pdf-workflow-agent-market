# Track 3 — Consumer Phantom Checkout
[auth]
keys = ["dev"]

[network]
allowAnyHost = true

[registry]
localAgents = ["/agents/*"]
includeCoralHomeAgents = true
localAgentRescanTimer = "5s"

[docker]
address = "host.docker.internal"
