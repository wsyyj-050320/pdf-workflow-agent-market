[project]
name = "helius-monitor"
version = "0.1.0"
description = "Coral agent that watches a Solana wallet via Helius and reports confirmed payments."
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
    "mcp>=1.0.0",
]
